/** Must match the `name` field in package.json — Homebridge resolves the plugin by it. */
export const PLUGIN_NAME = '@caseywebb/homebridge-free-sleep';

/** Must match the `pluginAlias` in config.schema.json and the `platform` key in config.json. */
export const PLATFORM_NAME = 'FreeSleep';
